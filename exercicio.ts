{
        let comprimento: number = 30;
        let largura: number = 15;
        let altura: number = 25;
        let volume = (comp:number,larg:number,alt:number=7) => (comprimento * largura * altura)
        
        
        console.log(`O volume é ${volume(comprimento,largura,altura)}`)
}